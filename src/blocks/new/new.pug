//- Все примеси в этом файле должны начинаться c имени блока (new)

mixin new(mods)

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' new--' + modsList[i].trim();
      }
    }

  section.new(class=allMods)
    .new__inner
      block

mixin new-item(mods)

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' new--' + modsList[i].trim();
      }
    }

  .new__item(class=allMods)&attributes(attributes)
    block

mixin new-item-img (href, src, mods)

  //- Принимает:
  //-   text   {string} - содержимое пункта
  //-   href   {string} - ссылка этого пункта (если пустая, то это не ссылка, а span)
  //-   mods   {string} - список модификаторов
  -
    // список модификаторов
      var allMods = '';
      if(typeof(mods) !== 'undefined' && mods) {
        var modsList = mods.split(',');
        for (var i = 0; i < modsList.length; i++) {
          allMods = allMods + ' socials--' + modsList[i].trim();
        }
      }

  a.new__img-wrap(class=allMods, href=href)&attributes(attributes)
    img.new__img(class=allMods, src=src)&attributes(attributes)

mixin new-item-content(title, subtitle, price, mods)

  //- Принимает:
  //-   text   {string} - содержимое пункта
  //-   href   {string} - ссылка этого пункта (если пустая, то это не ссылка, а span)
  //-   mods   {string} - список модификаторов
  -
    // список модификаторов
      var allMods = '';
      if(typeof(mods) !== 'undefined' && mods) {
        var modsList = mods.split(',');
        for (var i = 0; i < modsList.length; i++) {
          allMods = allMods + ' socials--' + modsList[i].trim();
        }
      }

  .new__content
    .new__item-title= title
    .new__item-subtitle= subtitle
    block
    .new__price= price
      span.b-rub &nbsp;Ь
